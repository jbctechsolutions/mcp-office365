{
  "mcpServers": {
    "office365-dev": {
      "command": "node",
      "args": ["dist/index.js", "--state-dir", "${HOME}/.mcp-office365-dev"],
      "env": {
        "OUTLOOK_MCP_CLIENT_ID": "${OUTLOOK_MCP_CLIENT_ID:-}",
        "OUTLOOK_MCP_TENANT_ID": "${OUTLOOK_MCP_TENANT_ID:-common}"
      },
      "description": "Local dev build (run npm run build first). State is isolated in ~/.mcp-office365-dev so a dev migration can never touch the shared ~/.mcp-office365 store (#99). tokens.json stays shared by design."
    }
  }
}
